import { PhotoRestockClient } from "@/components/pantry/photo-restock-client";

export const dynamic = "force-dynamic";

export default function PhotoRestockPage() {
  return <PhotoRestockClient />;
}
